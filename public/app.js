let data=null,selectedKey='my-health-story';const $=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));function showView(name){document.querySelectorAll('.view').forEach(v=>v.classList.toggle('hidden',v.id!=='view-'+name));document.querySelectorAll('[data-nav]').forEach(a=>a.classList.toggle('active',a.dataset.nav===name));window.scrollTo({top:0,behavior:'smooth'});history.replaceState(null,'','#'+name)}document.querySelectorAll('[data-nav]').forEach(a=>a.onclick=()=>showView(a.dataset.nav));function fmt(v){return v==null?'—':Number(v).toFixed(Math.abs(v)>=10?0:1)}function render(){if(!data)return;const ins=data.insights;const findings=ins?.findings||[];const rel=data.relationships||{};const cards=findings.slice(0,4);$('signalCount').textContent=findings.length;$('status').innerHTML='<i></i> '+(data.metadata?'Story built from '+data.metadata.eventCount+' observations':'Demo story');$('patientName').textContent=data.profile?.name||'Your health story';$('patientConcern').textContent=data.profile?.concern||'Longitudinal health story';$('changed').innerHTML=cards.map(f=>'<article class="kpi"><span>'+esc(f.metric)+'</span><strong>'+(f.changePct>0?'+':'')+fmt(f.changePct)+'%</strong><small>'+(f.baseline!=null&&f.recent!=null?'Baseline '+fmt(f.baseline)+' → recent '+fmt(f.recent)+' · ':'')+f.persistentDays+' observed days outside baseline</small></article>').join('')||'<p class="muted">No persistent deviations detected yet.</p>';$('storySummary').innerHTML=findings.length?'<p>PulseStory found <b>'+findings.length+'</b> persistent signal changes in the recent window.</p><p class="muted">The system then checks whether those changes overlap in time and asks you for context where the data cannot explain the change.</p>':'<p class="muted">Add health data to generate your story.</p>';const metrics=data.metrics||Object.keys((data.series||[]).reduce((a,d)=>{Object.keys(d.values||{}).forEach(k=>a[k]=1);return a},{}));
const metricLabels={resting_hr:'Resting HR',hrv:'HRV',sleep:'Sleep',steps:'Steps',activity:'Activity',glucose:'Glucose',temperature:'Temperature',stress:'Stress',respiratory_rate:'Respiratory rate',calories:'Calories',heart_rate:'Heart rate',oxygen_variation:'Oxygen variation',vo2_max:'VO₂ max',distance:'Distance',lh:'LH',estrogen:'Estrogen',pdg:'PdG',flow_volume:'Flow volume',fatigue:'Fatigue',sleep_issue:'Sleep issue',cramps:'Cramps',headaches:'Headaches',sore_breasts:'Sore breasts',mood_swing:'Mood swing',appetite:'Appetite',exercise_level:'Exercise level',stress_reported:'Reported stress',food_cravings:'Food cravings',indigestion:'Indigestion',bloating:'Bloating'};
const metricRows=metrics.map(metric=>{
  const points=(data.series||[]).map(d=>({date:d.date,value:d.values?.[metric]})).filter(x=>x.value!=null);
  if(!points.length)return '';
  const vals=points.map(x=>Number(x.value)),min=Math.min(...vals),max=Math.max(...vals),range=max-min||1;
  const bars=points.slice(-90).map(p=>'<div class="bar" title="'+esc(p.date)+' · '+esc(metricLabels[metric]||metric)+' '+fmt(p.value)+'"><i style="height:'+Math.max(8,Math.min(100,((p.value-min)/range)*92+8))+'%"></i></div>').join('');
  const latest=points[points.length-1];
  return '<div class="metric-row"><div class="metric-title"><b>'+esc(metricLabels[metric]||metric)+'</b><span>'+fmt(latest.value)+' · '+esc(latest.date)+'</span></div><div class="metric-chart">'+bars+'</div></div>';
}).join('');
$('chart').innerHTML=metricRows||'<p class="muted">No numeric observations found for this profile.</p>';const findingMap=Object.fromEntries(findings.map(f=>[f.metric,f]));const allChanges=data.changes||{},details=data.changeDetails||{};$('evidence').innerHTML=Object.entries(allChanges).filter(([,v])=>v!=null).sort((a,b)=>Math.abs(b[1])-Math.abs(a[1])).map(([metric,change])=>{const f=findingMap[metric],d=details[metric]||{},trend=d.trend7Pct;const context=d.baseline!=null&&d.recent!=null?'Baseline '+fmt(d.baseline)+' → recent '+fmt(d.recent)+(d.previous7!=null&&d.recent7!=null?' · last 7 days '+(trend>0?'+':'')+fmt(trend)+'% vs prior 7':''):'';return '<div class="evidence-item"><div><b>'+esc(metricLabels[metric]||metric)+'</b><small>'+esc(context)+(context?' · ':'')+(f?(f.persistentDays+' days outside threshold · '+esc(f.source||'measured')):'Observed change · not persistent by current rule')+'</small></div><strong class="'+(change>0?'change-up':'change-down')+'">'+(change>0?'+':'')+fmt(change)+'%</strong></div>'}).join('')||'<p class="muted">No numeric changes available for this story yet.</p>';$('pairs').innerHTML=(ins?.pairs||[]).length?(ins.pairs.map(p=>'<div class="evidence-pair"><b>'+p.overlapDays+' overlapping days</b><br>'+p.metrics.map(esc).join(' + ')+'</div>').join('')):'<p class="muted">As more signals are added, PulseStory will show which changes happened together.</p>';$('relationships').innerHTML=(rel.relationships||[]).length?rel.relationships.slice(0,20).map(p=>'<div class="evidence-pair"><b>'+esc(metricLabels[p.a]||p.a)+' ↔ '+esc(metricLabels[p.b]||p.b)+'</b><br><span>r = '+p.r+' · '+esc(p.strength)+' association · '+p.sampleSize+' paired observations'+(p.lagDays? ' · strongest at '+Math.abs(p.lagDays)+' day lag':'')+'</span></div>').join(''):'<p class="muted">Not enough overlapping data yet. Add more days of observations and PulseStory will test more relationships.</p>';$('briefChanges').innerHTML=findings.map(f=>'<li>'+esc(f.metric)+' changed '+(f.changePct>0?'+':'')+fmt(f.changePct)+'% from personal baseline and remained outside the baseline threshold for '+f.persistentDays+' observed days.</li>').join('')||'<li>No persistent deviations detected.</li>';$('briefTimeline').textContent=data.metadata?('Observed '+data.metadata.firstDate+' → '+data.metadata.lastDate+'. The recent window is compared with the preceding personal baseline period.'):'Demo timeline.';$('briefFollowup').textContent=findings.length?'The pattern above can be surfaced to a clinician as a possible area for further investigation. PulseStory does not prescribe or autonomously order tests.':'No follow-up suggestion generated from the current evidence.';$('provenance').innerHTML=(data.provenance||[]).map(p=>'<span>'+esc(p[0])+' · <b>'+esc(p[1])+'</b></span>').join('')}async function loadStoryProfile(){const base=$('profileKey')?.value||'my-health-story';const r=await fetch('/api/participants?profileKey='+encodeURIComponent(base));if(r.ok){const j=await r.json();if(j.participants?.length&&!j.participants.some(p=>p.profileKey===selectedKey))selectedKey=j.participants[0].profileKey;}}async function loadStory(){const r=await fetch('/api/health-story?profileKey='+encodeURIComponent(selectedKey));if(r.ok){data=await r.json();const ir=await fetch('/api/insights?profileKey='+encodeURIComponent(selectedKey));data.insights=ir.ok?await ir.json():null;const rr=await fetch('/api/relationships?profileKey='+encodeURIComponent(selectedKey));data.relationships=rr.ok?await rr.json():null}else{data={profile:{name:'Your health story',concern:'Upload your data to start'},series:[],changes:{},provenance:[]};data.insights=null}render()}$('interpret').onclick=async()=>{const answers=Object.fromEntries(['exercise','stress','meds','illness'].map(k=>[k,$(k).value]));const r=await (await fetch('/api/interview',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({answers,profileKey:selectedKey})})).json();$('interpretation').innerHTML='<div class="interpret"><b>Context added</b>'+r.factors.map(x=>'<p>• '+esc(x)+'</p>').join('')+'</div>'};$('aiInterpret').onclick=async()=>{const evidence={profile:data.profile,changes:data.changes,findings:data.insights?.findings,pairs:data.insights?.pairs,context:Object.fromEntries(['exercise','stress','meds','illness'].map(k=>[k,$(k).value]))};$('interpretation').innerHTML='<div class="interpret">Building your longitudinal story…</div>';const r=await (await fetch('/api/ai-interpret',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({evidence})})).json();$('interpretation').innerHTML='<div class="interpret"><b>Story synthesis</b><p>'+esc(r.interpretation||r.error)+'</p><p class="muted">Interpretation aid only; not a diagnosis.</p></div>'};$('exportPdf').onclick=()=>{window.location.href='/api/export-pdf?profileKey='+encodeURIComponent(selectedKey)};$('review').onclick=async()=>{const r=await (await fetch('/api/review-request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({profileKey:selectedKey})})).json();$('reviewMsg').innerHTML='<div class="success">✓ '+esc(r.message)+' Reference: '+esc(r.id)+'</div>'};$('uploadForm').onsubmit=async e=>{e.preventDefault();const files=[...$('csvFile').files];const payload={profileKey:$('profileKey').value||selectedKey,files:await Promise.all(files.map(async f=>({name:f.name,content:await f.text()})))};$('uploadResult').innerHTML='<div class="interpret">Adding '+files.length+' file(s) to your health story…</div>';const r=await fetch('/api/import-csv',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const j=await r.json();$('uploadResult').innerHTML='<div class="'+(r.ok?'success':'interpret')+'">'+(r.ok?'✓ Added '+j.eventsInserted+' observations from '+j.files.length+' file(s).':'✕ '+esc(j.error))+'</div>';if(r.ok){selectedKey=j.profileKey||selectedKey;await loadStory();showView('overview')}};$('profileKey')?.addEventListener('change',loadStoryProfile);async function init(){await loadStoryProfile();await loadStory();if(location.hash&&document.getElementById('view-'+location.hash.slice(1)))showView(location.hash.slice(1))}init();